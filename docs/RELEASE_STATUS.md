# v0.1.0 发布状态

更新时间：2026-08-27

## 已完成

- 无法确认再分发许可的第三方 Skills 和 MiniMax 提示词手册已移出发布包。
- H3 生成联系表、包含本地路径的指标文件、旧压缩包和运行缓存已移出发布包。
- 示例工作流已使用中性素材文件名，不再包含聊天附件参数和去水印指令。
- 技能文件读取与目录导入已限制在批准目录内，并有安全回归测试覆盖。
- 已补齐 GPL-3.0、MIT、Apache-2.0 声明、第三方来源和修改文件版权头。
- 已加入 `pyproject.toml`、`.comfyignore`、CI、统一测试脚本和发布审计工具。
- 本机 ComfyUI 基线下，核心 Python、导演台和前端结构测试均已通过。
- 最终发布树审计已通过，未发现缓存、私人路径、聊天参数、真实素材名或生成媒体。
- 已合并版本后缀节点模块并删除不可达兼容代码；完整回归同时通过 ComfyUI
  v0.33.2 与 v0.34.0。
- Git 已初始化，默认分支为 `main`，首版源码已完成本地提交。

## 外部节点依赖审计

首发版已经移除导演台中未验证的 MAINodes 动作修复、角色五视图和
H3-FaceRefine 开关、运行时调用及跳过式集成测试。发布包不再要求安装这两个节点包，
也不会按它们的注册名构建子图。

## 仍需维护者完成

- [x] GitHub 仓库 Owner 已填写为 `civilcoco`，作者显示名称为“沐阳Myang”。
- [x] Comfy Registry Publisher 已创建，Publisher ID 为 `civilcoco`，显示名称为“沐阳Myang”。
- [x] 已运行 `python tools/release_audit.py --strict-metadata`，严格审计通过。
- [ ] 在干净的 ComfyUI 环境中打开两个已清洗的示例工作流。
- [ ] 将本地 `main` 分支推送到 GitHub。
- [ ] 创建 `v0.1.0` 标签和 GitHub Release。
- [ ] 完成第一次手动 Comfy Registry 发布，再决定是否启用自动发布。
- [ ] 全球公开 H3 生成演示片段前，取得适当许可或 MiniMax 的书面说明。
